<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= subject %>
    </title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #232323;
            color: #ffffff;
        }

        .container {
            max-width: 600px;
            margin: 20px auto;
            padding: 20px;
            background-color: #1a1a1a;
            border-radius: 10px;
            border: 2px solid #A065FF;
            text-align: center;
        }

        .header img {
            max-width: 150px;
            height: auto;
            margin-bottom: 15px;
        }

        .title {
            font-size: 24px;
            font-weight: bold;
            color: #A065FF;
            margin-bottom: 20px;
        }

        .message {
            font-size: 16px;
            line-height: 1.5;
            color: #e0e0e0;
            margin-bottom: 20px;
        }

        .product {
            display: flex;
            align-items: center;
            background-color: #2a2a2a;
            padding: 10px;
            border-radius: 10px;
            margin-bottom: 20px;
            color: #ffffff;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.3);
        }

        .product img {
            width: 100px;
            height: 100px;
            object-fit: cover;
            border-radius: 10px;
            margin-right: 15px;
        }

        .product-info {
            flex: 1;
        }

        .product-name {
            font-size: 18px;
            font-weight: bold;
            color: #A065FF;
            margin: 5px 0;
        }

        .token {
            font-size: 16px;
            color: #A065FF;
            font-weight: bold;
            margin-top: 20px;
        }

        .footer {
            font-size: 12px;
            color: #bfbfbf;
            margin-top: 20px;
        }

        .footer a {
            color: #A065FF;
            text-decoration: none;
            font-weight: bold;
        }
    </style>
</head>

<body>
    <div class="container">
        <div class="header">
            <img src="https://res.cloudinary.com/dvpenabty/image/upload/v1733093069/logo_g8qfvy.png"
                alt="CyberGames Logo">
        </div>
        <div class="title">Product Claimed Successfully!</div>
        <p class="message">Congratulations! You have successfully claimed the following product:</p>
        <div class="product">
            <img src="<%= product.image[0] %>" alt="<%= product.name %>">
            <div class="product-info">
                <div class="product-name">
                    <%= product.name %>
                </div>
            </div>
        </div>
        <p class="token">Your token: <%= gameToken %>
        </p>
        <div class="footer">
            <p>Thank you for choosing CyberGames!</p>
            <p><a href="https://pf-ecommerce2024.vercel.app/">Visit our website</a></p>
        </div>
    </div>
</body>

</html>